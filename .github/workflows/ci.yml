name: CI Pipeline

on:
  push:
    branches: 
        - main
    
jobs:
    hello-job:
        runs-on: ubuntu-latest
        steps:
            - name: Say Hello
              run: echo "Hello CI/CD!"
